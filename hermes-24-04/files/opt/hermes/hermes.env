# Hermes Agent 1-Click notes
#
# Hermes user:  hermes
# Hermes home:  /home/hermes/.hermes
# Workspace:    /home/hermes/workspace
#
# Configure providers with:
#   sudo -u hermes -H /home/hermes/.local/bin/hermes setup
# or:
#   hermes setup
#
# Provider keys are stored in /home/hermes/.hermes/.env by Hermes itself.
#
# Pre-configured inference via Serverless Inference (optional):
#   MODEL_ACCESS_KEY    Model access key (Inference > Manage in the control panel)
#   INFERENCE_MODEL     Model id from GET https://inference.do-ai.run/v1/models
#                       (OpenAI-compatible serverless inference endpoint)
#
# On first boot, /opt/hermes/apply-inference-from-env.sh reads /etc/environment
# first, then this file. If MODEL_ACCESS_KEY is set, it lists models from
# the serverless inference API, configures Hermes for DigitalOcean Serverless Inference,
# and skips the interactive first-login setup wizard. If INFERENCE_MODEL is
# empty, the first chat-capable model from that API list is used.
#
# Droplet environment aliases still work: GRADIENT_KEY, MODEL_ACCESS_MODEL,
# DO_INFERENCE_MODEL, GRADIENT_MODEL.
MODEL_ACCESS_KEY=
INFERENCE_MODEL=
