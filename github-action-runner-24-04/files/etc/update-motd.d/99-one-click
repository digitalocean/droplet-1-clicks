#!/bin/sh
#
# Configured as part of the DigitalOcean 1-Click Image build process

myip=$(hostname -I | awk '{print$1}')

cat <<EOF
********************************************************************************

Welcome to DigitalOcean's 1-Click GitHub Actions Runner Droplet.

To keep this Droplet secure, the UFW firewall is enabled.
All ports are BLOCKED except 22 (SSH) and Docker-related ports.

This image includes:
  * GitHub Actions Runner (self-hosted)
  * Docker and Docker Compose (for container jobs)
  * Dedicated 'runner' user

--------------------------------------------------------------------------------
QUICK START
--------------------------------------------------------------------------------

1. On GitHub, create a self-hosted runner registration token:
     Repo/Org → Settings → Actions → Runners → New self-hosted runner

2. SSH in and run the setup wizard (also runs on first login):
     /etc/setup-github-runner.sh

   Or register manually:
     cd /home/runner/actions-runner
     sudo -u runner ./config.sh --url https://github.com/ORG/REPO --token TOKEN
     systemctl start actions-runner

--------------------------------------------------------------------------------
SERVICE MANAGEMENT
--------------------------------------------------------------------------------

  Status:   systemctl status actions-runner
  Start:    systemctl start actions-runner
  Stop:     systemctl stop actions-runner
  Restart:  systemctl restart actions-runner
  Logs:     journalctl -u actions-runner -f

  Runner home: /home/runner/actions-runner

--------------------------------------------------------------------------------
SECURITY NOTES
--------------------------------------------------------------------------------

  * Prefer private repositories with self-hosted runners.
  * Forks of public repos can run untrusted workflow code on this Droplet.
  * Keep the registration token private; it expires in about one hour.

Docs: https://docs.github.com/en/actions/hosting-your-own-runners

********************************************************************************
To delete this message of the day: rm -rf $(readlink -f ${0})
EOF
