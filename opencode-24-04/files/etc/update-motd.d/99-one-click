#!/bin/sh
#
# Configured as part of the DigitalOcean 1-Click Image build process

cat <<EOF
********************************************************************************

Welcome to DigitalOcean's One-Click OpenCode Droplet.

OpenCode is an open-source AI coding agent that runs in your terminal. Use
natural language to write, debug, and refactor code. This droplet is
pre-configured to use DigitalOcean Serverless Inference.

MODELS (via DigitalOcean Serverless Inference):
  Listed live at first login from GET https://inference.do-ai.run/v1/models
  after you enter a DigitalOcean model access key. Switch later with /models.

GETTING STARTED:

  1. If MODEL_ACCESS_KEY was passed as a droplet environment variable, Serverless
     Inference is configured automatically. Otherwise, on first login the setup wizard
     will prompt for your DigitalOcean model access key and let you pick a
     default model (or the Intelligent Inference Router). Create a key at:
     https://cloud.digitalocean.com/gen-ai
     (API Keys > Model Access Keys)

  2. Run OpenCode:
     cd /path/to/your/project
     opencode

Intelligent Inference Router: create a router in the control panel attached to
your model access key, set DO_INFERENCE_ROUTER=<name>, and re-run the setup
wizard to route each prompt to the best-fit model (uses the same key, model
"router:<name>").

Helper Commands:
  Check version:    /opt/opencode-version.sh
  Update OpenCode:  /opt/update-opencode.sh
  Re-run setup:     /opt/setup-opencode.sh

Other providers: use /connect inside OpenCode to add your own API keys
                 for Anthropic, OpenAI, Google, and 70+ more providers.

Configuration:  /root/.config/opencode/opencode.json
Documentation:  https://opencode.ai/docs

********************************************************************************
To delete this message of the day: rm -rf $(readlink -f ${0})
EOF
