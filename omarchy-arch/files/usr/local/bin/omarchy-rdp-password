#!/bin/bash
# Set the desktop/RDP password, and start the remote desktop with it.
# Also how you bring the desktop back after a reboot, since the password is
# held in RAM and a reboot deliberately forgets it.
exec /usr/local/bin/omarchy-droplet-setup "$@"
