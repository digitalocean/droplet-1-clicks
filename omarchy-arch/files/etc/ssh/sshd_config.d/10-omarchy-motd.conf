# Arch ships PrintMotd no (99-archlinux.conf); re-enable so the per-instance
# onboot MOTD (connection instructions + desktop password) is shown on login.
# sshd uses the first-obtained value, and this drop-in sorts before 99-.
PrintMotd yes
